#include "main.h"
#include <stdio.h>

/**
 * rev_string - check the code
 *
 * Return: Always 0.
 */

void rev_string(char *s)
{
	int len = strlen(s);
	char tmp;
	
	while
		(start < end) 
		{
        		tmp = s[start];
        		s[start] = s[end];
        		s[end] = tmp;
        		start++;
        		end--;
		}
}
